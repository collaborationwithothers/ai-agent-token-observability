name: Unsafe Pull Request Azure Login Fixture

on:
  pull_request:

permissions:
  contents: read
  id-token: write

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: azure/login@v2
      - run: terraform plan -input=false
