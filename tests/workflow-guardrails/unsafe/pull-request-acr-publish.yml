name: Unsafe ACR Image Publish

on:
  pull_request:

permissions:
  contents: read
  id-token: write

jobs:
  publish:
    runs-on: ubuntu-latest
    steps:
      - run: docker push tokenobs.azurecr.io/product-api:${GITHUB_SHA}
